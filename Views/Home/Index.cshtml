@model IEnumerable<Kipa_plus.Models.Kisa>

@{
    ViewData["Title"] = "Kisat";
}

<div class="grid-container">

    @foreach (var item in Model)
    {
        <div class="grid-item" onClick="location.href='/Kisa/Edit?id@item.id'">@item.Name</div>
    
    }
</div>
